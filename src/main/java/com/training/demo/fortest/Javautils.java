package com.training.demo.fortest;

import java.time.DayOfWeek;
import java.time.LocalDate;
import java.util.stream.Collectors;
import java.util.stream.Stream;

import static java.util.stream.Collectors.groupingBy;

public class Javautils {


    public static void print(int stard, int end) {
//
//        var start = LocalDate.of(stard, 1, 3);
//        var endDate = LocalDate.of(end, 12, 13);
//        var stream = Stream.iterate(start, localDate -> localDate.plusMonths(1))
//                .filter(localDate -> localDate.getDayOfWeek() == DayOfWeek.FRIDAY)
//                .collect(groupingBy(LocalDate::getYear));

        


    }


}
